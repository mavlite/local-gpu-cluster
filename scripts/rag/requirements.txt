# Python dependencies for scripts/rag/refresh.py and migrate_backfill.py.
# Install into the existing scraper venv:
#   /opt/vcf-scraper-venv/bin/pip install -r scripts/rag/requirements.txt
PyYAML>=6.0
requests>=2.31
trafilatura>=1.8
feedparser>=6.0
